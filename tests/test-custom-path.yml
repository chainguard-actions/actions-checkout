name: Test checkout to custom path
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - id: action
        uses: .
        with:
          repository: actions/hello-world-javascript-action
          ref: main
          path: my-checkout-dir
      - name: Assert files exist in custom path
        run: |
          if [ ! -d "$GITHUB_WORKSPACE/my-checkout-dir" ]; then
            echo "FAIL: custom path directory 'my-checkout-dir' not found"
            exit 1
          fi
          if [ ! -f "$GITHUB_WORKSPACE/my-checkout-dir/action.yml" ]; then
            echo "FAIL: action.yml not found in custom path"
            exit 1
          fi
          echo "PASS: files found in custom path"
      - name: Assert commit output is set
        env:
          COMMIT: ${{ steps.action.outputs.commit }}
        run: |
          echo "commit: $COMMIT"
          if [ -z "$COMMIT" ]; then
            echo "FAIL: expected commit output to be non-empty"
            exit 1
          fi
          echo "PASS: commit output is '$COMMIT'"
